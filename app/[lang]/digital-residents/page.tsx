import { RoutePage, type LocalePageProps } from "../route-page";

export default function DigitalResidentsPage({ params }: LocalePageProps) {
  return <RoutePage pageId="digital-residents" params={params} />;
}
