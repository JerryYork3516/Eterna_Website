import { RoutePage, type LocalePageProps } from "../../route-page";

export default function AftellePage({ params }: LocalePageProps) {
  return <RoutePage pageId="product-aftelle" params={params} />;
}
