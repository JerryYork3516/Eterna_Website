# Local: run `npm run dev`; no application environment variables are currently required.
# Preview: keep Preview-only values in the Vercel Preview environment.
# Production: keep Production values isolated in Vercel Production; never copy them here or into Preview.
# Vercel owns its system environment variables; do not define them in this file.
# Contact and Analytics are disabled, so they intentionally declare no variables.
